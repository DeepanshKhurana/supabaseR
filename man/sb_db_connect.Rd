% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sb_db_connect.R
\name{sb_db_connect}
\alias{sb_db_connect}
\title{Connect to Supabase}
\usage{
sb_db_connect(schema = Sys.getenv("SUPABASE_SCHEMA", "public"))
}
\arguments{
\item{schema}{The schema name}
}
\value{
Invisible connection object
}
\description{
Uses DBI/PostgreSQL to connect directly to your Supabase database.
Get connection parameters from the Connect tab in your Supabase Dashboard
(Project Settings > Database > Connection Strings). All three modes work:
Direct Connection, Transaction Pooler, and Session Pooler. If Direct connection
doesn't work, try Transaction or Session Pooler. Remember to use the correct
host, user, and password from your chosen connection mode.
}
\examples{
\dontrun{
# Connect using environment variables
sb_db_connect()

# Connect with a specific schema
sb_db_connect(schema = "public")

# Disconnect when done
sb_db_disconnect()
}
}
