% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sb_api_query.R
\name{sb_api_query}
\alias{sb_api_query}
\title{Query a table via API}
\usage{
sb_api_query(
  table = NULL,
  columns = "*",
  where = NULL,
  limit = 0,
  schema = get_schema()
)
}
\arguments{
\item{table}{The table name}

\item{columns}{Columns to select (default: all). Character vector or \code{"*"}.}

\item{where}{A named list for filtering. Supports operators via nested lists:
\code{list(id = 1)} for equality, \code{list(age = list(gt = 25))} for \code{age > 25}.
Operators: eq, neq, gt, gte, lt, lte, like, ilike, in, is}

\item{limit}{Maximum rows to return (0 for all)}

\item{schema}{The schema name}
}
\value{
A \code{tibble}
}
\description{
Query a table via API
}
\examples{
\dontrun{
sb_api_connect()

# Select specific columns
sb_api_query("users", columns = c("id", "name"))

# Filter with a simple equality condition
sb_api_query("users", where = list(status = "active"))

# Filter with operators
sb_api_query("orders", where = list(
  status = "pending",
  total = list(gte = 100)
))

# Available operators: eq, neq, gt, gte, lt, lte, like, ilike, in, is

# Limit results
sb_api_query("users", limit = 5)
}
}
