% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sb_api_upsert.R
\name{sb_api_upsert}
\alias{sb_api_upsert}
\title{Upsert rows into a table via API}
\usage{
sb_api_upsert(
  table = NULL,
  data = NULL,
  conflict_columns = NULL,
  schema = get_schema()
)
}
\arguments{
\item{table}{The table name}

\item{data}{A data frame of rows to upsert}

\item{conflict_columns}{Column(s) to check for conflicts (e.g., primary key)}

\item{schema}{The schema name}
}
\value{
Number of rows affected (invisibly)
}
\description{
Insert rows, or update on conflict with specified columns.
}
\examples{
\dontrun{
sb_api_connect()

# Upsert a single row (insert or update on conflict)
sb_api_upsert(
  "users",
  data.frame(id = 1, name = "Alice Updated"),
  conflict_columns = "id"
)

# Upsert multiple rows with a composite key
sb_api_upsert(
  "orders",
  data.frame(
    user_id  = c(1, 2),
    order_id = c(10, 11),
    status   = c("shipped", "pending")
  ),
  conflict_columns = c("user_id", "order_id")
)
}
}
