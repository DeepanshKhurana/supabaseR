% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sb_db_upsert.R
\name{sb_db_upsert}
\alias{sb_db_upsert}
\title{Upsert rows into a table}
\usage{
sb_db_upsert(
  table = NULL,
  data = NULL,
  conflict_columns = NULL,
  schema = get_schema()
)
}
\arguments{
\item{table}{The table name}

\item{data}{A data frame of rows to upsert}

\item{conflict_columns}{Column(s) to check for conflicts (e.g., primary key)}

\item{schema}{The schema name}
}
\value{
Number of rows affected (invisibly)
}
\description{
Insert rows, or update if conflict on specified columns.
}
\examples{
\dontrun{
# Upsert: insert or update on conflict
sb_db_upsert(
  "users",
  data = data.frame(id = 1, name = "Alice", email = "alice@new.com"),
  conflict_columns = "id"
)

# Upsert multiple rows with composite key
sb_db_upsert(
  "order_items",
  data = data.frame(
    order_id = c(1, 1),
    product_id = c(10, 20),
    quantity = c(5, 3)
  ),
  conflict_columns = c("order_id", "product_id")
)
}
}
