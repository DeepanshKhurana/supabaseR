# Get table schema

Get table schema

## Usage

``` r
sb_schema(table = NULL, schema = get_schema())
```

## Arguments

- table:

  The table name

- schema:

  The schema name

## Value

A data frame with column info
